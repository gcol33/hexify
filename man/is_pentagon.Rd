% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/hex_topology.R
\name{is_pentagon}
\alias{is_pentagon}
\title{Detect Pentagon Cells}
\usage{
is_pentagon(cell_id, grid)
}
\arguments{
\item{cell_id}{Cell IDs to check. Numeric for ISEA, character for H3.}

\item{grid}{A HexGridInfo or HexData object specifying the grid.}
}
\value{
A logical vector. \code{TRUE} for pentagon cells, \code{FALSE} for hexagons.
}
\description{
Identifies which cells are pentagons. Any hexagonal tiling of the sphere
must contain exactly 12 pentagons (at the icosahedron vertices).
Pentagon cells have 5 neighbors instead of 6.
}
\details{
\strong{H3 backend:} Uses the vendored H3 \code{isPentagon} function.

\strong{ISEA backend:} The 12 pentagons are located at icosahedron vertices.
At any resolution, the pentagon cells are those whose IJ coordinates
are (0, 0) within the 12 vertex quads (quads 0 and 11 for the poles,
and specific cells in quads 1-10).
}
\examples{
\donttest{
# H3 pentagon detection
g <- hex_grid(resolution = 1, type = "h3")
cells <- grid_global(g)
pent <- is_pentagon(cells$cell_id, g)
sum(pent)  # Should be 12
}
}
\seealso{
\code{\link[=get_neighbors]{get_neighbors()}} for neighbor finding (pentagons have 5 neighbors)
}
