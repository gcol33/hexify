% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/hexify_compat.R
\name{dgGEO_to_SEQNUM}
\alias{dgGEO_to_SEQNUM}
\title{Convert geographic coordinates to cell sequence numbers (dggridR-compatible)}
\usage{
dgGEO_to_SEQNUM(dggs, in_lon_deg, in_lat_deg)
}
\arguments{
\item{dggs}{Grid specification from dgconstruct() or hexify_construct()}

\item{in_lon_deg}{Longitude vector in degrees}

\item{in_lat_deg}{Latitude vector in degrees}
}
\value{
List with component:
\item{seqnum}{Cell indices (character vector)}
}
\description{
Compatibility wrapper for hexify_geo_to_cell() that follows dggridR naming
conventions and return format. Converts lon/lat coordinates to cell indices.
}
\examples{
\dontrun{
dggs <- dgconstruct(area = 1000, aperture = 3)

# Single point
result <- dgGEO_to_SEQNUM(dggs, in_lon_deg = 0, in_lat_deg = 0)
print(result$seqnum)

# Multiple points
lons <- c(0, 10, 20)
lats <- c(0, 45, -30)
result <- dgGEO_to_SEQNUM(dggs, lons, lats)
print(result$seqnum)
}
}
\seealso{
\code{\link{hexify_geo_to_cell}}
}
